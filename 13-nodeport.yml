apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: NodePort
  selector:
    name: service-test
    environment: dev
    project: expense
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80  
