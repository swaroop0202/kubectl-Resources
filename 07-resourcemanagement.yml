apiVersion: v1
kind: Pod
metadata:
  name: resource-demo
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    resources:
      requests:
        memory: "100mi"
        cpu: "128m"
      limits:
        memory: "100mi"
        cpu: "128m"